import { Box, Typography } from '@mui/material'

const DeviceDetail = (props: {}) => {
  return (
    <Box>
      <Typography variant="h4">DeviceDetail</Typography>
    </Box>
  );
}

export default DeviceDetail;
